<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demeter.mapper.RecordsMapper">
    <select id="getRecords" parameterType="map" resultType="com.demeter.model.RecordModel">
        SELECT * FROM demeter.records
        WHERE userid = #{userid}
        <if test="status != 0 ">
            AND status = #{status}
        </if>
        ORDER BY applydate DESC
        LIMIT #{currentIndex} , #{pageSize}
    </select>

    <select id="getRecordsCount" parameterType="map" resultType="Integer">
        SELECT COUNT(*) FROM demeter.records
        WHERE userid = #{userid}
        <if test="status != 0 ">
            AND status = #{status}
        </if>
    </select>
</mapper>